/**
 * Minimal BufferGeometryUtils stub — only implements toTrianglesDrawMode.
 * Full implementation available in Three.js examples/addons.
 */
export function toTrianglesDrawMode(geometry, drawMode) {
    // Return geometry unchanged — most models use regular triangles
    return geometry;
}
